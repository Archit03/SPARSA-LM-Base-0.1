datasets:

  # Local Train Dataset
  - name: local_train
    type: local
    config:
      path: "C:/Users/ASUS/Desktop/PreProcessed/processed/split/train"
      patterns:
        - "*.txt"
        - "*.csv"
      csv_text_column: "text"  # Specify the column to extract text if using CSV files

  # Local Test Dataset
  - name: local_test
    type: local
    config:
      path: "C:/Users/ASUS/Desktop/PreProcessed/processed/split/test"
      patterns:
        - "*.txt"
        - "*.csv"
      csv_text_column: "text"  # Specify the column to extract text if using CSV files
